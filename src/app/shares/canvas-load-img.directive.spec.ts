import { CanvasLoadImgDirective } from './canvas-load-img.directive';

describe('CanvasLoadImgDirective', () => {
  it('should create an instance', () => {
    const directive = new CanvasLoadImgDirective();
    expect(directive).toBeTruthy();
  });
});
